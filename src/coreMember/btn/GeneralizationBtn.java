package coreMember.btn;

import coreMember.btn.mode.Mode;

public class GeneralizationBtn extends FuncBtn {
    public GeneralizationBtn(String name, Mode mode)
    {
        super(name, mode);
    }
}
